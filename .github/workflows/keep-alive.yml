name: Keep Backend Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # Pings every 14 minutes
  workflow_dispatch:      # Allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -s https://bidpulse-2k7i.onrender.com/act/health
